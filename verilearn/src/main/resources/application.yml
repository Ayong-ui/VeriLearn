spring:
  application:
    name: verilearn
  datasource:
    url: jdbc:mysql://localhost:3306/verilearn?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always

server:
  port: 8080

verilearn:
  ai:
    base-url:
    api-key:
    model:
  feishu:
    app-id:
    app-secret:
    verification-token:
  schedule:
    daily-task-cron: "0 0 8 * * ?"

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
